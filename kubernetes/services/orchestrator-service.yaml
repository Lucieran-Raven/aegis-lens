apiVersion: v1
kind: Service
metadata:
  name: orchestrator-service
  namespace: aegis-lens
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: orchestrator
