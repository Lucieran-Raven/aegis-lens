apiVersion: v1
kind: Service
metadata:
  name: hr-dashboard-service
  namespace: aegis-lens
  labels:
    app: hr-dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hr-dashboard
