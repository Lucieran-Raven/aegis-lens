apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: aegis-lens
  labels:
    app: redis
    component: cache
spec:
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis
  type: ClusterIP
