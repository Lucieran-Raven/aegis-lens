apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: aegis-lens
  labels:
    app: postgres
    component: database
spec:
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres
  type: ClusterIP
