apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-dashboard
  namespace: aegis-lens
  labels:
    app: hr-dashboard
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hr-dashboard
  template:
    metadata:
      labels:
        app: hr-dashboard
        version: v1
    spec:
      containers:
      - name: hr-dashboard
        image: aegis-lens/hr-dashboard:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        envFrom:
        - configMapRef:
            name: hr-dashboard-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
